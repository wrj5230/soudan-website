---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
---

<Layout>
  <Header />

  <!-- 全シーンを重ねて配置 -->
  <div class="scenes">

    <!-- Scene 0: Hero -->
    <div class="scene active" data-scene="0">
      <canvas id="hero-canvas"></canvas>
      <div class="scene-content hero-content">
        <p class="hero-label">SOUDAN</p>
        <h1 class="hero-title">祈りを、<br />創る。</h1>
        <p class="hero-sub">あなたの想いをカタチに。</p>
      </div>
    </div>

    <!-- Scene 1: Concept -->
    <div class="scene" data-scene="1">
      <div class="scene-content">
        <p class="section-label">Concept</p>
        <h2 class="scene-title">SOUDANとは。</h2>
        <div class="divider"></div>
        <div class="sou-container">
          <svg class="sou-svg" viewBox="0 0 360 340" preserveAspectRatio="none">
            <defs>
              <mask id="circleMask">
                <rect width="360" height="340" fill="white"/>
                <circle cx="180" cy="44"  r="47" fill="black"/>
                <circle cx="44"  cy="236" r="47" fill="black"/>
                <circle cx="316" cy="236" r="47" fill="black"/>
              </mask>
            </defs>
            <g mask="url(#circleMask)">
              <line x1="180" y1="44"  x2="44"  y2="236" stroke="rgba(201,168,76,0.3)" stroke-width="1"/>
              <line x1="180" y1="44"  x2="316" y2="236" stroke="rgba(201,168,76,0.3)" stroke-width="1"/>
              <line x1="44"  y1="236" x2="316" y2="236" stroke="rgba(201,168,76,0.3)" stroke-width="1"/>
            </g>
          </svg>
          <!-- 創: 上中央 -->
          <button class="sou-circle" data-idx="0" style="top:0;left:50%;transform:translateX(-50%)">
            <span class="sou-kanji">創</span>
          </button>
          <!-- 想: 左下 -->
          <button class="sou-circle" data-idx="1" style="bottom:60px;left:0">
            <span class="sou-kanji">想</span>
          </button>
          <!-- 添: 右下 -->
          <button class="sou-circle" data-idx="2" style="bottom:60px;right:0">
            <span class="sou-kanji">添</span>
          </button>
          <!-- 説明テキスト -->
          <div class="sou-desc-panel" id="sou-desc-panel">
            <p class="sou-desc-text" id="sou-desc-text"></p>
          </div>
        </div>
      </div>
    </div>

    <!-- Scene 2: Craftsmen -->
    <div class="scene craftsmen-scene" data-scene="2">
      <div class="scene-content">
        <p class="section-label">Craftsmen & Techniques</p>
        <h2 class="scene-title">職人と、技法。</h2>
        <div class="divider"></div>
        <div class="techniques-grid">
          <div class="technique-card">
            <h3>蒔絵</h3>
            <p>漆の上に金銀の粉を蒔いて描く、日本が誇る伝統装飾技法。繊細な文様があなたの想いを永遠に刻みます。</p>
          </div>
          <div class="technique-card">
            <h3>漆塗り</h3>
            <p>何十もの工程を経て積み重ねられる漆の層。深みのある艶と、百年を超える耐久性を持ちます。</p>
          </div>
          <div class="technique-card">
            <h3>彩色</h3>
            <p>岩絵具や金泥を用いた日本画の技法。仏の世界を彩る色彩が、祈りの空間に命を吹き込みます。</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Scene 3: Configurator CTA -->
    <div class="scene cfg-scene" data-scene="3">
      <div class="scene-content cfg-content">
        <p class="section-label">Try the Configurator</p>
        <h2 class="cfg-title">あなただけの仏壇を、<br />創ってみる。</h2>
        <p class="cfg-sub">色・技法・素材を自由に組み合わせて、<br />あなたの祈りの空間をイメージしてください。</p>
        <a href="/configurator" class="cfg-btn" id="cfg-link">3Dで試してみる →</a>
      </div>
    </div>


  </div><!-- /.scenes -->

  <!-- ナビゲーション -->
  <div class="scene-nav" id="nav-dots"></div>

  <!-- スプラッシュ -->
  <div id="splash">
    <p class="splash-label">SOUDAN</p>
  </div>

</Layout>

<script>
  import * as THREE from 'three';
  import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js';

  // ── ヒーローカルーセル ──────────────────────────────────
  const canvas = document.getElementById('hero-canvas') as HTMLCanvasElement;
  const renderer = new THREE.WebGLRenderer({ canvas, antialias: true });
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  renderer.setSize(window.innerWidth, window.innerHeight);

  const threeScene = new THREE.Scene();
  threeScene.background = new THREE.Color('#080814');
  threeScene.fog = new THREE.FogExp2('#080814', 0.07);

  const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 100);

  // ライト
  threeScene.add(new THREE.AmbientLight(0xffffff, 0.25));
  const dirLight = new THREE.DirectionalLight(0xffd4a0, 1.4);
  dirLight.position.set(3, 6, 4);
  threeScene.add(dirLight);
  const rimLight = new THREE.DirectionalLight(0x4060c0, 0.5);
  rimLight.position.set(-4, 2, -5);
  threeScene.add(rimLight);

  // カラープリセット [外枠/背板色, 扉色]
  const PRESETS: [string, string][] = [
    ['#0c0c0c', '#8b1a1a'],  // 漆黒 × 朱
    ['#1a3a6b', '#c9a84c'],  // 群青 × 金
    ['#5c1a2e', '#c9a84c'],  // 深緋 × 金
    ['#1a4a2e', '#c9a84c'],  // 常磐 × 金
    ['#8b5e1a', '#1a2a4a'],  // 飴色 × 藍鉄
    ['#1a2a4a', '#8b1a1a'],  // 藍鉄 × 朱
    ['#0d0d18', '#1a3a6b'],  // 黒 × 群青
  ];

  const COUNT = PRESETS.length;
  const getRadius = () => window.innerWidth < 768 ? 2.0 : 2.8;
  const carousel = new THREE.Group();
  threeScene.add(carousel);
  const clones: THREE.Object3D[] = [];

  function makeMat(hex: string) {
    return new THREE.MeshPhysicalMaterial({
      color: new THREE.Color(hex),
      roughness: 0.2,
      metalness: 0.05,
      clearcoat: 1.0,
      clearcoatRoughness: 0.15,
    });
  }

  new GLTFLoader().load('/altar.glb', (gltf) => {
    const source = gltf.scene;

    // GLB読み込み完了後にスプラッシュをフェードアウト
    setTimeout(() => {
      document.getElementById('splash')?.classList.add('done');
    }, 2000);

    for (let i = 0; i < COUNT; i++) {
      const [outerHex, doorHex] = PRESETS[i];
      const clone = source.clone(true);

      clone.traverse((node: any) => {
        if (!node.isMesh) return;
        const n: string = node.name.toLowerCase();
        node.material = (n.includes('door') || n.includes('curtain'))
          ? makeMat(doorHex)
          : makeMat(outerHex);
      });

      const angle = (i / COUNT) * Math.PI * 2;
      const r = getRadius();
      clone.position.set(Math.sin(angle) * r, 0, Math.cos(angle) * r);
      carousel.add(clone);
      clones.push(clone);
    }
  });

  function updateCamera() {
    const mobile = window.innerWidth < 768;
    camera.fov    = mobile ? 58 : 45;
    camera.aspect = window.innerWidth / window.innerHeight;
    camera.position.set(0, mobile ? 0.8 : 1.6, mobile ? 4.2 : 5.5);
    camera.lookAt(0, mobile ? 0.1 : 0.3, 0);
    camera.updateProjectionMatrix();
    renderer.setSize(window.innerWidth, window.innerHeight);
    // カルーセル半径を更新
    const r = getRadius();
    clones.forEach((clone, i) => {
      const angle = (i / COUNT) * Math.PI * 2;
      clone.position.set(Math.sin(angle) * r, 0, Math.cos(angle) * r);
    });
  }
  updateCamera();
  window.addEventListener('resize', updateCamera);

  (function animate() {
    requestAnimationFrame(animate);
    carousel.rotation.y += 0.0016;
    // 各仏壇がカルーセルの回転を打ち消して常に正面を向く
    carousel.children.forEach(child => {
      child.rotation.y = Math.PI - carousel.rotation.y;
    });
    renderer.render(threeScene, camera);
  })();
</script>

<script>
  const scenes = document.querySelectorAll<HTMLElement>('.scene');
  const dotsContainer = document.getElementById('nav-dots')! as HTMLElement;
  const total = scenes.length;
  let current = 0;
  let animating = false;

  // ドット生成
  scenes.forEach((_, i) => {
    const dot = document.createElement('button');
    dot.className = 'nav-dot' + (i === 0 ? ' active' : '');
    dot.setAttribute('aria-label', `シーン ${i + 1}`);
    dot.addEventListener('click', e => { e.stopPropagation(); goTo(i); });
    dotsContainer.appendChild(dot);
  });

  function updateNav() {
    dotsContainer.querySelectorAll('.nav-dot').forEach((d, i) =>
      d.classList.toggle('active', i === current)
    );
  }

  function goTo(n: number) {
    if (animating || n === current || n < 0 || n >= total) return;
    animating = true;
    scenes[current].classList.remove('active');
    scenes[current].classList.add('leave');
    // フェードアウト完了後に次シーンをフェードイン
    setTimeout(() => {
      document.querySelectorAll('.scene.leave').forEach(s => s.classList.remove('leave'));
      current = n;
      scenes[current].classList.add('active');
      updateNav();
      setTimeout(() => { animating = false; }, 800);
    }, 500);
  }

  // タップ/クリックで次へ（フォーム・ボタン・リンクは除外）
  document.querySelector('.scenes')!.addEventListener('click', e => {
    const target = e.target as HTMLElement;
    if (target.closest('a, button, input, textarea, label, form')) return;
    goTo(current + 1);
  });

  // キーボード
  document.addEventListener('keydown', e => {
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') goTo(current + 1);
    if (e.key === 'ArrowUp' || e.key === 'ArrowLeft') goTo(current - 1);
  });

  // スワイプ（横）
  let touchStartX = 0;
  document.addEventListener('touchstart', e => { touchStartX = e.touches[0].clientX; }, { passive: true });
  document.addEventListener('touchend', e => {
    const dx = touchStartX - e.changedTouches[0].clientX;
    if (Math.abs(dx) < 50) return;
    if (dx > 0) goTo(current + 1);
    else goTo(current - 1);
  });

  // SOUトライアングル：クリックで説明フェードイン
  const souDescs = [
    '量産品ではなく、あなたの家にしかない祈りの空間を。職人の手で、ゼロから生み出します。',
    '故人の好きだったもの、家の佇まい、残された記憶。すべてを丁寧に聴き取り、仏壇に宿らせます。',
    '発注して終わりではなく、完成まで職人があなたに寄り添いながら、ともに創り上げます。',
  ];
  const souCircles = document.querySelectorAll<HTMLButtonElement>('.sou-circle');
  const souDescPanel = document.getElementById('sou-desc-panel')!;
  const souDescText = document.getElementById('sou-desc-text')!;

  souCircles.forEach(btn => {
    btn.addEventListener('click', e => {
      e.stopPropagation();
      const idx = Number(btn.dataset.idx);
      const alreadyActive = btn.classList.contains('active');
      souCircles.forEach(b => b.classList.remove('active'));
      if (alreadyActive) {
        souDescPanel.classList.remove('visible');
        return;
      }
      btn.classList.add('active');
      souDescPanel.classList.remove('visible');
      souDescText.textContent = souDescs[idx];
      requestAnimationFrame(() => {
        requestAnimationFrame(() => souDescPanel.classList.add('visible'));
      });
    });
  });

</script>

<style>
  /* ─── ベース ─── */
  :global(html), :global(body) {
    height: 100%;
    overflow: hidden;
  }

  .scenes {
    position: fixed;
    inset: 0;
  }

  /* ─── シーン ─── */
  .scene {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    pointer-events: none;
    transition: opacity 1.1s cubic-bezier(0.22,1,0.36,1);
    background: var(--color-bg);
  }
  .scene.active {
    opacity: 1;
    pointer-events: auto;
    z-index: 1;
  }
  .scene.leave {
    opacity: 0;
    z-index: 0;
  }

  .scene-content {
    max-width: 1000px;
    width: 100%;
    padding: 0 48px;
  }

  .scene-title {
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    font-weight: 300;
    letter-spacing: 0.1em;
    margin-bottom: 0;
  }

  /* ─── Hero ─── */
  #hero-canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }
  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
  }
  .hero-label {
    font-family: 'Cormorant Garamond', serif;
    font-size: 0.9rem;
    letter-spacing: 0.5em;
    color: #c9a84c;
    margin-bottom: 24px;
  }
  .hero-title {
    font-size: clamp(3.5rem, 8vw, 6rem);
    font-weight: 300;
    letter-spacing: 0.15em;
    line-height: 1.3;
    margin-bottom: 24px;
  }
  .hero-sub {
    font-size: 1rem;
    letter-spacing: 0.3em;
    color: #8a8a9a;
  }

  /* ─── Concept ─── */
  .sou-container {
    position: relative;
    width: min(360px, 100%);
    aspect-ratio: 360 / 340;
    margin-top: 32px;
  }
  .sou-svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }
  .sou-circle {
    all: unset;
    position: absolute;
    width: 88px;
    height: 88px;
    border-radius: 50%;
    border: 1px solid rgba(201,168,76,0.35);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: border-color 0.3s, background 0.3s, box-shadow 0.3s;
    background: rgba(10,8,6,0.4);
  }
  .sou-circle:hover { border-color: rgba(201,168,76,0.7); }
  .sou-circle.active {
    border-color: #c9a84c;
    background: rgba(201,168,76,0.08);
    box-shadow: 0 0 18px rgba(201,168,76,0.15);
  }
  .sou-kanji {
    font-family: 'Cormorant Garamond', 'Noto Serif JP', serif;
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 300;
    color: #c9a84c;
    line-height: 1;
    letter-spacing: 0.05em;
  }
  .sou-desc-panel {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    text-align: center;
    opacity: 0;
    transform: translateY(8px);
    transition: opacity 0.5s ease, transform 0.5s ease;
    pointer-events: none;
    height: 72px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .sou-desc-panel.visible {
    opacity: 1;
    transform: translateY(0);
  }
  .sou-desc-text {
    font-size: 0.82rem;
    line-height: 1.9;
    color: #d0cac0;
    max-width: 300px;
    margin: 0 auto;
  }

  /* ─── Craftsmen ─── */
  .craftsmen-scene { background: #0d1628; }
  .techniques-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 28px;
    margin-top: 8px;
  }
  .technique-card {
    border: 1px solid rgba(201,168,76,0.2);
    padding: 28px 24px;
  }
  .technique-card h3 {
    font-size: 1.1rem;
    letter-spacing: 0.15em;
    color: #c9a84c;
    margin-bottom: 12px;
  }
  .technique-card p { color: #d0cac0; line-height: 1.9; font-size: 0.85rem; }

  /* ─── Configurator CTA ─── */
  .cfg-scene {
    background: linear-gradient(135deg, #0a0f20 0%, #080814 60%, #0d1a2e 100%);
  }
  .cfg-content { text-align: center; }
  .cfg-title {
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 300;
    letter-spacing: 0.1em;
    line-height: 1.6;
    margin: 16px 0 24px;
  }
  .cfg-sub {
    font-size: 0.9rem;
    color: var(--color-muted);
    letter-spacing: 0.08em;
    line-height: 2;
    margin-bottom: 40px;
  }
  .cfg-btn {
    display: inline-block;
    border: 1px solid var(--color-gold);
    color: var(--color-gold);
    font-size: 0.85rem;
    letter-spacing: 0.2em;
    padding: 16px 48px;
    transition: all 0.3s;
  }
  .cfg-btn:hover { background: var(--color-gold); color: #080814; }


  /* ─── ナビゲーション ─── */
  .scene-nav {
    position: fixed;
    bottom: 32px;
    left: 0;
    right: 0;
    z-index: 100;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
  }
  :global(.nav-dot) {
    width: 6px; height: 6px;
    border-radius: 3px;
    background: rgba(201,168,76,0.3);
    border: none; cursor: pointer;
    transition: all 0.4s cubic-bezier(0.22,1,0.36,1);
    padding: 0;
    flex-shrink: 0;
    -webkit-tap-highlight-color: transparent;
  }
  :global(.nav-dot.active) {
    width: 22px;
    background: #c9a84c;
  }

  /* ─── スプラッシュ ─── */
  #splash {
    position: fixed;
    inset: 0;
    background: #080814;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    opacity: 1;
    transition: opacity 1.4s ease;
  }
  #splash.done { opacity: 0; }
  .splash-label {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.1rem;
    letter-spacing: 0.65em;
    color: #c9a84c;
    opacity: 0;
    animation: splashIn 0.9s ease 0.2s forwards;
  }
  @keyframes splashIn {
    to { opacity: 1; }
  }

  /* ─── Mobile ─── */
  @media (max-width: 768px) {
    .sou-container { max-width: 300px; }
    .sou-desc-text { font-size: 0.78rem; max-width: 240px; }
    .scene-content { padding: 0 20px; }
    .scene-nav { bottom: max(24px, calc(env(safe-area-inset-bottom, 0px) + 12px)); }
    .hero-title { letter-spacing: 0.08em; }
    .hero-sub { letter-spacing: 0.15em; font-size: 0.9rem; }
    .techniques-grid { grid-template-columns: 1fr; gap: 12px; overflow-y: auto; max-height: 60svh; }
    .technique-card { padding: 18px; }
  }
</style>
